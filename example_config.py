# Directory containing the SN template spectra.
sn_spec_dir: "/template-spectra"

# Path to the file containing the measurements for each SN.
sn_measure_path: "/measurements.fits"

# Path to the file containing info of the SN.
sn_info_path: "/spectrum_parameters.fits"

# Define the features to be measured.
# Either specify "Ia" or provide a path to an astropy
# readable table (in ecsv format)
features: "Ia"
